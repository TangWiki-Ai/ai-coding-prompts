# AI Coding 笔试速通：6 段提示词

适用场景：AI Coding 笔试、已有工程改造题、README 需求实现题、带测试反馈的开发题。

核心原则：不要让 AI 一路自由发挥。你要把题目拆成 6 个步骤，每一步都让 AI 输出具体结果，再进入下一步。

## 整体流程

1. 需求对齐：读 README、读代码、形成共同理解。
2. 架构设计：整理可执行的开发 spec。
3. 拆分 + 开发：把 spec 拆成 tickets，一次只实现一个功能点。
4. Code Review：对照 ticket 和 spec 检查实现质量。
5. 验收测试：对照 README、ticket 和 spec 看完成度。
6. 定点修复：根据测试反馈找根因，小范围修改。

---

## 第 1 步：需求对齐

使用时机：刚拿到题目，还没有开始写代码。

目标：先明确题目要什么。本阶段不写代码。

```text
请先阅读 README、项目目录、已有测试、入口文件、核心代码。

本阶段不要写代码，只做需求理解。

请持续追问我这个计划、设计或想法，直到我们达成清晰的共同理解。

请把它拆成一棵“决策树”：每个关键决定会引出后续决定。你要按轮次提问。每轮只问当前前置条件已经明确的问题，不要问依赖未决答案的问题。

每个问题请包含：
- 问题编号和简短标题
- 问题正文
- 你的推荐答案
- 为什么你推荐这个答案

你负责查事实。凡是可以通过阅读代码、文件、文档、配置或工具获得的信息，你自己去查，不要问我。

但决定必须由我来做。你不能替我决定，也不能默认我同意。

每轮提问后，等待我的回答。根据我的回答更新决策树，再进入下一轮。

当你认为所有关键分支都已经问完时，请总结我们达成的理解，并问我是否确认。

在我明确确认之前，不要开始写代码、改文件或执行实现。
```

---

## 第 2 步：架构设计

使用时机：需求已经对齐，需要形成后续开发依据。

目标：让 AI 先讲清项目怎么跑，再决定怎么改。

```text
请先阅读题目说明、README、项目目录、已有测试、入口文件和核心代码。

本阶段不要写代码，只整理一份可执行的开发 spec。

请重点回答：

1. 题目目标
用简洁语言说明这个题目最终要实现什么。

2. 已知信息
列出 README、代码和测试中已经明确的信息，包括输入输出、业务规则、已有模块、运行方式和测试方式。

3. 需求拆解
把题目要求拆成清晰的功能点。
每个功能点都要说明：要做什么、影响哪里、如何判断完成。

4. 实现思路
说明你建议怎么改。
包括需要关注的模块、核心流程、数据结构、接口关系和边界情况。
不要开始写代码。

5. 测试与验收
说明应该如何验证完成。
优先使用项目已有测试方式。
列出需要运行的测试命令、关键测试场景和验收标准。

6. 不做什么
明确哪些内容不要做，避免超范围实现。

7. 风险点
列出最容易出错的地方，比如边界条件、兼容性、隐藏测试、异常输入或项目约定。

输出后请等待我确认，再进入实现阶段。
```

---

## 第 3 步：拆分 + 开发

使用时机：已经有 spec，需要把工作拆小。

目标：把 spec 拆成多个小 ticket，一次只让 AI 完成一个。

### 把 spec 拆成 tickets

```text
请根据这份 spec 拆成一组可执行的开发 tickets。不要重新讨论需求，不要修改 spec，不要开始写代码。

要求：
- 每个 ticket 要足够小，适合一个 AI Coding 会话完成。
- 每个 ticket 都要有明确边界、验收标准和测试要求。
- 按依赖顺序排列：基础 → 核心 → 边界 → 收尾。
- 测试跟对应实现放在同一个 ticket 里。
- 每个 ticket 都要对应 spec 的具体部分，并遵守“不做什么”。

请先简要说明拆分思路、执行顺序和最大风险点。

然后按下面格式输出：

Ticket <编号>：<标题>

目标
这个 ticket 要完成什么。

背景
它来自 spec 的哪部分，为什么需要做。

修改范围
预计影响哪些模块、页面、接口、状态、数据结构或测试。

任务
列出具体要做的事。

验收标准
完成后应该满足什么，必须可检查。

测试要求
需要增加或修改哪些测试。只测试外部行为，不测试实现细节。

依赖
依赖哪些 ticket。没有就写“无”。

不包含
这个 ticket 明确不做什么。

如果发现 spec 有缺口，不要自行补完。请单独列出“需要确认的问题”，但仍然先拆出当前能确定的 tickets。
```

### 实现单个 ticket

```text
请实现 Ticket <编号>。

只处理这个 ticket 的范围，不要顺手实现其他 ticket。

实现时请遵守：
- 对照 spec 和当前 ticket 的验收标准。
- 优先使用项目已有模式和工具。
- 测试跟实现一起完成。
- 不做无关重构。
- 不扩大需求范围。

完成后请输出：
1. 修改了什么
2. 如何验证
3. 已运行的测试命令和结果
4. 仍然存在的风险或待确认问题
```

---

## 第 4 步：Code Review

使用时机：实现完一个 ticket 后，马上检查，不要等到最后一起看。

目标：先测小块，再测模块，最后做联调。

```text
请对刚才实现的改动做 code review。

重点检查：
1. 是否符合对应 ticket 和 spec
2. 是否有漏实现或超范围实现
3. 是否有 bug、边界问题或回归风险
4. 测试是否覆盖关键行为
5. 是否符合项目已有代码风格和架构模式

请先列出问题，按严重程度排序，并给出文件位置和修改建议。

没有问题也请明确说明。
```

---

## 第 5 步：验收测试

使用时机：Code Review 通过后，准备确认需求完成度。

目标：对照 README 和 spec 逐条验收。先给结论，不急着改。

```text
请做一次验收测试。

本阶段只验收，不直接改代码。

请对照 README、ticket 和 spec，逐条列出需求验收表。

验收表至少包含：
- 需求
- 验收标准
- 实现位置
- 测试证据
- 当前状态

当前状态请从下面选：
- 已实现且有运行证据
- 静态看起来满足但待验证
- 部分实现
- 未实现
- 失败
- 阻塞

请先给总体结论：当前是否可以交付。

注意：测试通过，不代表需求完成。必须逐条对照题目要求。

如果发现问题，只记录：
- 问题现象
- 证据
- 影响范围
- 建议修复位置
- 优先级

不要直接修复。先把问题讲清楚，等待我确认后再进入下一步。
```

---

## 第 6 步：定点修复

使用时机：测试或线上反馈显示通过率低，需要修一个具体问题。

目标：先定位根因，再做最小修复。

```text
现在只修一个具体问题。

先定位根因，再做最小修复。

请先输出失败卡片：
- 问题现象
- 错误类型
- 影响需求
- 已知证据
- 合理推断
- 未知信息

然后列出最多 3 个根因假设，每个假设都要写清楚支持证据和验证方式。

本轮只选择一个最可能、影响面最小的假设来修。

修复前，优先写一个能复现问题的测试、脚本或手工验证步骤。

修复时只改必要代码，不改整体架构，不加无关功能。

改完后给我复测命令。

在我贴回运行结果前，不要说已修复，只能说待验证。
```

---

## 最后提醒

这套流程能帮你拿到 AI Coding 笔试的基本完成度。

它的价值，是把 AI 从自由发挥，变成按步骤交付：先对齐需求，再形成 spec，再拆 tickets，再实现、review、验收和定点修复。

但如果你想冲高分，不能只靠模板。你还是要结合具体题目，理解代码结构、业务规则和隐藏边界。

模板解决的是流程稳定性，真正拉开差距的是判断力。
